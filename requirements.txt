# ==============================================================================
# PROJETO: SIMULADOR TRIBUTÁRIO 2027 (REFORMA EC 132/2023 & LC 123/2006)
# NOTE: Este repositório é um projeto Node.js/TypeScript. Não há dependências
# Python listadas aqui. Este arquivo foi convertido para um requirements.txt
# compatível com o pip (apenas comentários) para evitar erros ao executar
# `pip install -r requirements.txt` em CI que erroneamente tenta instalar
# dependências Python.
#
# Dependências JS/TS devem ser instaladas através do package.json:
#   npm install
# ou
#   pnpm install
# ou
#   yarn install
#
# Se você precisa de bibliotecas Python para algum workflow específico, adicione
# apenas nomes de pacotes Python válidos nesta lista (um por linha), por exemplo:
#   requests>=2.31.0
#   pytest>=7.4.0
#
# ==============================================================================
